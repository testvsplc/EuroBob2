import {
    Dimensions,
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    errorHolder: {
        marginTop: 20,
        paddingLeft: 25,
        paddingRight: 25,
        alignItems: 'center'
    }
});

export default styles;
